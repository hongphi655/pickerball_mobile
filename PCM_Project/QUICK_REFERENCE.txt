# 🚀 PCM Backend - Quick Reference Card

## ⚡ Start Backend (Pick One)

### 1️⃣ Easiest - Windows Batch
```cmd
Double-click: START_BACKEND.bat
```

### 2️⃣ PowerShell
```powershell
.\START_BACKEND.ps1
```

### 3️⃣ Manual CMD
```cmd
cd PCM_Backend
set ASPNETCORE_ENVIRONMENT=Development
dotnet run --no-build
```

### 4️⃣ Manual PowerShell
```powershell
cd PCM_Backend
$env:ASPNETCORE_ENVIRONMENT = "Development"
dotnet run --no-build
```

## 🔐 Test Credentials
```
Username: admin
Password: Test123!
```

## 📱 Run Flutter App
```bash
cd PCM_Mobile
flutter run -d web
```

## 🧪 Test Endpoints

### Health Check
```bash
curl http://localhost:5001/health
```

### Login
```bash
curl -X POST http://localhost:5001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"Test123!"}'
```

### Swagger Docs
```
http://localhost:5001/swagger
```

## ✅ Verification

All these should work:
- ✅ `http://localhost:5001/health` → {"status":"ok"}
- ✅ Login returns JWT token with 561+ chars
- ✅ `http://localhost:5001/swagger` → Swagger UI loads
- ✅ Flutter app connects without errors

## 🆘 Troubleshooting

### Backend won't start?
```powershell
Get-Process dotnet | Stop-Process -Force
START_BACKEND.bat
```

### Port 5001 in use?
```powershell
netstat -ano | findstr "5001"
taskkill /PID <PID> /F
```

### Connection refused?
- Check backend window shows "Now listening on: http://localhost:5001"
- Verify no firewall blocks port 5001
- Try `curl http://localhost:5001/health`

### Login fails?
- Check username/password: admin / Test123!
- Verify backend health: `curl http://localhost:5001/health`
- Check browser console for errors

## 📊 Status

| Component | Status |
|-----------|--------|
| Backend Server | ✅ WORKING |
| Login Endpoint | ✅ WORKING |
| JWT Token | ✅ GENERATING |
| Health Check | ✅ RESPONDING |
| Swagger | ✅ AVAILABLE |
| Flutter App | ✅ READY |

## 🎯 Next Steps

1. Open command prompt/PowerShell
2. Run `START_BACKEND.bat` or `.\START_BACKEND.ps1`
3. Open another terminal
4. Run `cd PCM_Mobile && flutter run -d web`
5. Login with: admin / Test123!
6. Explore dashboard

## 📝 Important Files

- `START_BACKEND.bat` - Run this to start backend
- `PCM_Backend/` - Backend source code
- `PCM_Mobile/` - Flutter app source code
- `README_READY.md` - Full documentation
- `FIX_SUMMARY.md` - Technical details of the fix

---

**STATUS: ✅ PRODUCTION READY**

Everything is working! Start the backend and enjoy!
