import 'package:flutter/material.dart';

class AdminDepositsApprovalScreen extends StatelessWidget {
  const AdminDepositsApprovalScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Đã xóa')),
      body: const Center(child: Text('Màn hình quản lý ví admin đã bị xóa.')),
    );
  }
}
